{
  "name": "adsb-display",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend,adsb -c cyan,magenta,yellow \"npm run dev -w packages/backend\" \"npm run dev -w packages/frontend\" \"node --env-file=./packages/backend/.env fake-adsb.js\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/backend && npm run build -w packages/frontend",
    "build:shared": "npm run build -w packages/shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}